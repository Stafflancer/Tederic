{% set classes = [
  field_name|clean_class,
] %}

<div{{ attributes.addClass(classes) }}>
  {% for item in items %}
    <div class="image-carousel-wrapper">
      {{ item.content }}
    </div>
  {% endfor %}
</div>
