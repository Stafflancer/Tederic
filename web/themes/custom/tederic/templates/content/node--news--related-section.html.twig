<article{{ attributes }}>
  <a href="{{ url }}" class="item">

    <div class="wrapper">
      <div class="img">{{ content.field_image_crop }}</div>
      <div class="content">
        {{ label }}

        <div class="content-bottom">
          {% if content.field_special_label.0 %}
            {{ content.field_special_label }} · {{ content.field_date }}
          {% else %}
            {{ content.field_date }}
          {% endif %}
        </div>
      </div>
    </div>

  </a>
</article>
