{%
  set classes = [
    paragraph.bundle|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished'
  ]
%}

<div{{ attributes.addClass(classes) }}>
  {% if content.field_solutions.0 %}
    <h3>{{ 'Related Applications & solutions'|t }}</h3>
    {{ content.field_solutions }}
  {% endif %}
</div>
